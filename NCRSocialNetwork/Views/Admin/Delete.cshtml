@model NCRSocialNetwork.Models.ClubEvent

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>ClubEvent</legend>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.ClubEventsId)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.ClubEventsId)
    </div>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.Club.ClubName)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Club.ClubName)
    </div>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.Event.EventTitle)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Event.EventTitle)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
